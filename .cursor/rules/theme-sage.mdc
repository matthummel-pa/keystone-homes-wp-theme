---
description: Sage 11 theme, CPTs, PageCopy fields, and Blade conventions
globs: app/**/*.php,resources/views/**/*.blade.php
alwaysApply: false
---

# Theme (Sage 11)

Stack: Blade + Acorn + Tailwind v4 + Vite 8. Design CSS is `resources/css/keystone.css`, not ad-hoc Tailwind utility soup.

## Custom post types

Registered in `app/post-types.php`. Constants: `Catalog::LISTING` (`listing`), `Catalog::BOOKING` (`booking`), `Catalog::AGENT` (`agent`). ThemeForest / WP.org would move this to a companion plugin — see `marketplace.mdc`. Do not extract CPTs unless Matt asks to package for a store.

- Rewrites: `/listing/{slug}/`, `/agent/{slug}/`. Bookings are not public. CPT `query_var` is false so `?listing=` is not a WP query var.
- Booking preselect: `?listing_id={post_id}` (composer also reads legacy `?listing=`). Never use `?listing=` as a public query — it 404s `/book/` when the CPT query var is registered.
- All custom fields go through `Catalog::getMeta` / `updateMeta` → keys are `ks_{field}`.
- Featured flag: store `1` or omit. Do **not** store `"0"` and treat it as false — `(bool) '0'` is true in PHP. `featuredListings()` uses `! empty($item['featured'])`.
- Listing agent is `ks_listing_agent` (agent post ID), not a slug.
- Showings POST to `keystone/v1/bookings` and create Booking posts (`requested` → `confirmed` → `completed`).

## Page copy

Marketing page text is post meta on the WordPress page, schema in `app/Support/PageCopy.php`.

- Blade should use `{!! ($copy['field'] ?? '') ?: 'fallback' !!}` so empty meta does not blank the H1 and a missing key does not 500 on PHP 8.
- `PageCopy::all()` needs a real page ID. On the static front page that is `page_on_front`. Do not return `[]` when the queried object ID is 0 if you still need home defaults.
- Allowed HTML in copy: `em`, `strong`, `br`, `a`.
- Templates (also set as `_wp_page_template`):

| Slug | Template |
| --- | --- |
| home | `front-page.blade.php` |
| listings | `page-listings.blade.php` |
| book | `page-book.blade.php` |
| areas | `page-areas.blade.php` |
| guide | `page-guide.blade.php` |
| agents | `page-agents.blade.php` |
| contact | `page-contact.blade.php` |
| blog | `home.blade.php` |

## Query / templates

Do **not** set `is_page` / `is_singular` without a real `WP_Post`. Sage / `post-template.php` will 500 (`ID` on null). If you swap a template for a virtual route, keep the query non-singular and use `template_include`.

After Blade edits, if the page looks stale: `wp acorn view:clear --path="$HOME/wp" --allow-root`.

## Admin

Field-only editing (`app/admin.php` metaboxes). Do not reintroduce blocks or the site editor as the editing path.

Image fields use the media library (`resources/js/admin-media.js`): page `hero_image`, listing/agent `image`, post featured image. Store a URL in `ks_*` (Unsplash hotlinks still work). Uploads also set the featured image. Do not go back to a textarea “Photo URL” for those.
